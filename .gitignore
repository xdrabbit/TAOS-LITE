node_modules
.next
out
build
dist
coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env.local
.env*.local
*.tsbuildinfo
.DS_Store

# Predictive-typing model artifacts (generated by scripts/build-predict-model.mjs;
# the deployed app serves them from Supabase, so they need not be committed).
public/models
# Local offline fixtures contain real private conversation data — never commit.
scripts/fixtures

# macOS / SMB share junk
._*
.smbdelete*
*.swp
